import { NextRequest, NextResponse } from "next/server";
import { serverConfig } from "@/lib/config";
const text = (value: unknown, limit: number) => typeof value === "string" ? value.trim().slice(0, limit) : "";
const email = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
const variants = new Set(["private-presentation", "brochure", "project-details", "general-enquiry"]);
const fail = (status: number) => NextResponse.json({ ok: false, error: "We couldn’t submit your request right now. Please try again." }, { status });
export async function POST(request: NextRequest) { try { if (!request.headers.get("content-type")?.includes("application/json")) return fail(415); const size = Number(request.headers.get("content-length") ?? 0); if (size > 8_000) return fail(413); const origin = request.headers.get("origin"); if (origin && origin !== request.nextUrl.origin) return fail(403); const body: unknown = await request.json(); if (!body || typeof body !== "object") return fail(400); const values = body as Record<string, unknown>; if (text(values.website, 1)) return NextResponse.json({ ok: true }); const name = text(values.name, 100); const phone = text(values.phone, 24).replace(/[\s()-]/g, ""); const mail = text(values.email, 160); const source = text(values.source, 80); const variant = text(values.variant, 40); if (name.length < 2 || !/^(?:\+?\d{1,3})?\d{10}$/.test(phone) || (mail && !email.test(mail)) || !source || !variants.has(variant)) return fail(400); const lead = { name, phone, email: mail || undefined, requirement: text(values.requirement, 80) || undefined, source, variant, pathname: text(values.pathname, 180), referrer: text(values.referrer, 300), utm_source: text(values.utm_source, 120), utm_medium: text(values.utm_medium, 120), utm_campaign: text(values.utm_campaign, 120), utm_content: text(values.utm_content, 120), utm_term: text(values.utm_term, 120), timestamp: new Date().toISOString() }; if (!serverConfig.leadWebhookUrl) return NextResponse.json({ ok: false, configured: false, error: "We couldn’t submit your request right now. Please try again." }, { status: 503 }); const response = await fetch(serverConfig.leadWebhookUrl, { method: "POST", headers: { "Content-Type": "application/json", ...(serverConfig.leadApiSecret ? { Authorization: `Bearer ${serverConfig.leadApiSecret}` } : {}) }, body: JSON.stringify(lead), signal: AbortSignal.timeout(8000) }); return response.ok ? NextResponse.json({ ok: true }) : fail(502); } catch { return fail(500); } }
