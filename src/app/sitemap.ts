import type { MetadataRoute } from "next";
import { insights, seoPages } from "@/data/seo";
import { siteUrl } from "@/lib/seo";
export default function sitemap(): MetadataRoute.Sitemap { if (!siteUrl) return []; const image = `${siteUrl}/images/presente/exterior/presente-exterior-wide.jpeg`; return [{ url: siteUrl, changeFrequency: "weekly", priority: 1, images: [image] }, { url: `${siteUrl}/insights`, changeFrequency: "weekly", priority: .7, images: [image] }, { url: `${siteUrl}/privacy`, changeFrequency: "yearly", priority: .2 }, { url: `${siteUrl}/disclaimer`, changeFrequency: "yearly", priority: .2 }, ...seoPages.map((page) => ({ url: `${siteUrl}/${page.slug}`, changeFrequency: "monthly" as const, priority: .8, images: [image] })), ...insights.map((article) => ({ url: `${siteUrl}/insights/${article.slug}`, lastModified: article.updatedDate, changeFrequency: "monthly" as const, priority: .6, images: [image] }))]; }
